Ето един професионално оформен `README.md` файл, който е готов за копиране директно в GitHub. Той описва проекта, технологиите и дава стъпка по стъпка инструкции за стартиране.

Можеш да създадеш файл с име **README.md** в главната папка на проекта (`D:\DistributedImageLab\`) и да поставиш това съдържание вътре.

---

# Distributed Image Lab 🖼️☁️

**Distributed Image Lab** е разпределена система за асинхронна обработка на изображения. Проектът демонстрира архитектурния модел **Producer-Consumer**, използвайки **ASP.NET Core** за уеб интерфейс, **Redis** за опашка от съобщения и **Python** за тежката обработка на изображенията.

## 🚀 Функционалности

* **Асинхронна комуникация:** Уеб сървърът не блокира по време на обработката.
* **Обработка на изображения:**
* **Филтри:** Grayscale, Sepia, Invert, Blur, Sharpen, Emboss, Contour.
* **Трансформации:** Завъртане (90°, -90°, 180°).
* **Воден знак (Watermark):** Добавяне на потребителски текст върху изображението.


* **Реално време:** Автоматично обновяване на галерията (Client-side Polling).
* **Външни услуги:** Интеграция с API за прогноза на времето (`wttr.in`).
* **Docker Integration:** Лесно пускане на инфраструктурата (Redis).

## 🛠️ Технологичен стек

| Компонент | Технология | Описание |
| --- | --- | --- |
| **Frontend / Producer** | C# / ASP.NET Core 8 MVC | Приема файлове и команди, праща JSON задачи към Redis. |
| **Backend / Consumer** | Python 3 + Pillow | Слуша Redis опашката и обработва изображенията. |
| **Message Broker** | Redis | In-memory база данни за опашката от задачи (`image_queue`). |
| **Infrastructure** | Docker Compose | Контейнеризация на Redis и Redis Commander. |

---

## 📋 Изисквания (Prerequisites)

Преди да стартирате проекта, уверете се, че имате инсталирани:

1. **Docker Desktop** (за пускане на Redis).
2. **.NET 8 SDK** (за WebApp).
3. **Python 3.x** (за Worker-а).

---

## 🏁 Инструкции за стартиране (Getting Started)

За да пуснете системата, трябва да изпълните StartALL.bat илида отворите **3 отделни терминала**.

### Стъпка 1: Стартиране на Инфраструктурата (Redis) 🗄️

В първия терминал навигирайте до главната папка и стартирайте Docker контейнерите:

```bash
cd DistributedImageLab/project
docker compose up

```

*Забележка: Ако използвате по-стара версия на Docker, командата може да е `docker-compose up`.*

---

### Стъпка 2: Стартиране на Уеб Приложението (WebApp) 🌐

Във втория терминал стартирайте ASP.NET приложението:

```bash
cd DistributedImageLab/project/WebApp
dotnet restore
dotnet run

```

Сайтът ще бъде достъпен на: `http://localhost:5xxx` (вижте терминала за точния порт).

---

### Стъпка 3: Стартиране на Работника (Python Worker) 🐍

В третия терминал настройте и пуснете Python скрипта:

```bash
cd DistributedImageLab/project/Worker

# 1. Създаване на виртуална среда (препоръчително)
python -m venv venv

# 2. Активиране на средата
# За Windows:
.\venv\Scripts\activate
# За Mac/Linux:
# source venv/bin/activate

# 3. Инсталиране на библиотеките
pip install redis pillow

# 4. Стартиране
python worker.py

```

---

## 🎮 Как се използва

1. Отворете браузъра на адреса на WebApp (напр. `http://localhost:5167`).
2. Качете изображение от формата.
3. Изберете желаните трансформации (завъртане, ефект).
4. (Опционално) Въведете текст за "Воден знак".
5. Натиснете бутона **"🚀 ИЗПЪЛНИ ВСИЧКО"**.
6. Снимката ще се обработи на заден план и ще се появи автоматично в галерията долу.

## 📂 Структура на проекта

```text
DistributedImageLab/
├── docker-compose.yml       # Конфигурация за Redis
├── project/
│   ├── WebApp/              # ASP.NET Core MVC Проект
│   │   ├── Controllers/     # Логика за качване и Redis
│   │   ├── Views/           # HTML Интерфейс
│   │   └── wwwroot/uploads/ # Споделена папка за снимки
│   └── Worker/              # Python Проект
│       └── worker.py        # Скрипт за обработка на изображения

